# Database
MYSQL_ROOT_PASSWORD=CHANGE_ME
AUTH_DB=authdb
TEAM_DB=teamsdb
TASK_DB=tasksdb

# JWT
JWT_SECRET=CHANGE_ME_LONG_RANDOM
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=168h

# Internal service token
INTERNAL_TOKEN=CHANGE_ME_INTERNAL

# Kafka
KAFKA_BROKERS=dev_kafka:9092

# SMTP (dev)
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_FROM=no-reply@todo.local

# Realtime
PORT_REALTIME=8086
ALLOWED_WS_ORIGINS=http://localhost,http://localhost:5173
WS_ALLOW_USERID_QUERY=true
